# 知识库功能使用指南

## 概述

知识库是一个私人知识管理系统，让您可以：

- 📝 上传和存储知识文档
- 🔍 快速查询所需信息
- 💬 通过对话获取答案
- 📚 管理个人知识积累

## 功能特点

### 1. 知识管理

- 支持 Markdown 格式
- 分类和标签管理
- 知识条目列表展示
- 响应式设计，移动端友好

### 2. 上传知识

- 直观的表单界面
- Markdown 内容支持
- 自动生成文件
- 下载后手动添加到项目

### 3. 智能问答

- 交互式聊天界面
- 对话历史保存
- 上下文理解
- 清除历史功能

### 4. 持久化存储

- 使用 LocalStorage 保存对话
- 浏览器本地存储
- 跨会话持久化

## 使用方法

### 添加新知识

1. **打开上传界面**

   - 访问知识库页面 `/knowledge`
   - 点击"上传知识"按钮

2. **填写知识信息**

   - **标题**：知识的简短标题（必填）
   - **内容**：详细内容，支持 Markdown 格式（必填）
   - **分类**：知识的分类（选填）
   - **标签**：用逗号分隔的多个标签（选填）

3. **保存知识**
   - 点击"上传"按钮
   - 系统会自动下载一个 `.md` 文件
   - 将下载的文件放到 `src/content/knowledge/` 目录
   - 重新构建网站以查看新知识

### 查询知识

1. **浏览知识列表**

   - 知识条目按时间倒序排列
   - 显示标题、摘要、分类和标签
   - 点击任意条目查看详情

2. **使用问答助手**

   - 在右侧聊天框输入问题
   - 点击"发送"或按回车键
   - 查看助手的回答
   - 对话历史会自动保存

3. **管理对话历史**
   - 对话记录保存在浏览器本地
   - 点击"清除历史"删除所有对话
   - 刷新页面不会丢失历史记录

## 文件结构

```
src/
├── content/
│   └── knowledge/           # 知识库文件目录
│       └── welcome.md       # 欢迎文档
├── components/
│   └── knowledge/           # 知识库组件
│       ├── KnowledgeList.tsx      # 列表组件
│       ├── KnowledgeUpload.tsx    # 上传组件
│       ├── KnowledgeChat.tsx      # 聊天组件
│       └── index.ts
├── pages/
│   ├── knowledge.astro      # 知识库主页
│   └── knowledge/
│       └── [...slug].astro  # 知识详情页
└── pages/api/knowledge/     # API 路由（服务器模式）
    ├── upload.ts
    └── query.ts
```

## Markdown 文件格式

每个知识文件使用 YAML frontmatter 格式：

```markdown
---
title: '知识标题'
date: 2026-01-25
summary: '简短摘要'
category: '分类名称'
tags: ['标签1', '标签2', '标签3']
---

# 标题

这里是内容...

## 子标题

更多内容...
```

## 高级功能

### 启用服务器模式（可选）

如果部署到支持 SSR 的服务器（如 Vercel、Netlify），可以启用服务器模式：

1. **修改配置**

   ```js
   // astro.config.js
   export default defineConfig({
     output: 'hybrid', // 或 'server'
     // ...其他配置
   })
   ```

2. **功能增强**
   - 上传直接保存到服务器
   - 无需手动添加文件
   - 可以实现更复杂的搜索
   - 可以集成 AI 服务

### 集成 AI 服务

可以将问答功能与 AI API 集成：

- OpenAI GPT
- Claude API
- 本地 LLM
- 向量数据库搜索

## 常见问题

### Q: 为什么上传后需要手动添加文件？

A: 因为网站部署在 GitHub Pages 等静态托管服务上，无法直接修改服务器文件。下载的文件需要通过 Git 提交到仓库。

### Q: 对话历史保存在哪里？

A: 对话历史保存在浏览器的 LocalStorage 中，只在当前浏览器可用。清除浏览器数据会删除历史记录。

### Q: 如何实现更智能的问答？

A: 可以集成 AI API 服务，或者部署到支持服务器端运行的平台，然后修改 `KnowledgeChat.tsx` 中的查询逻辑。

### Q: 能否导入/导出知识库？

A: 所有知识都是 Markdown 文件，可以直接复制 `src/content/knowledge/` 目录进行备份或迁移。

## 技术栈

- **Astro**: 静态站点生成
- **React**: 交互式组件
- **TypeScript**: 类型安全
- **Tailwind CSS**: 样式框架
- **Framer Motion**: 动画效果
- **React Toastify**: 通知提示

## 贡献

欢迎提交问题和改进建议！

## 许可证

遵循项目主许可证。
